#include <stdio.h>

float smpInt(float p, float r, float t) {
    return (p * r * t) / 100;
}

int main() {

    float P = 10000, R = 12, T = 1, SI;
    
    SI = smpInt(P, R, T);

    printf("Simple Interest: %.2f\n", SI);

    return 0;
}
