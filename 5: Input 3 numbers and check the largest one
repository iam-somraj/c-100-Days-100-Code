#include <stdio.h>

void main()
{
    int n1,n2,n3,max;
    printf("Enter 3 numbers:");
    scanf("%d %d %d", &n1, &n2, &n3);
    
    if(n1>n2 && n1>n3)
    max = n1;
    else if(n2>n1 && n2>n3)
    max = n2;
    else
    max = n3;
    
    printf("The largest the number is %d", max);
}
